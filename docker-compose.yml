services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: oniria-web:latest
    container_name: oniria-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
